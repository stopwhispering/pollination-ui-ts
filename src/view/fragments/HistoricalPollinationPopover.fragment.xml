<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core">

    <Popover
		placement="Auto"
		showHeader="false"
		horizontalScrolling="false"
		verticalScrolling="false"
		contentWidth="290px"
		>

        <VBox>
            <Label text="ongoing"/>
            <Text text="{popoverModel>/ongoing}"/>
        </VBox>
        
        <VBox>
            <Label text="pollination_status"/>
            <Label text="{popoverModel>/pollination_status}"/>
        </VBox>
        
        <VBox>
            <Label text="pollination_at"/>
            <Text text="{popoverModel>/pollination_at}"/>
        </VBox>
        
        <VBox>
            <Label text="reverse"/>
            <Text text="{popoverModel>/reverse}"/>
        </VBox>
<!-- 
        <FormattedText
                            htmlText="{
                                parts: [
                                    'popoverModel>/pollination_status',
                                    'popoverModel>/ongoing',
                                    'popoverModel>/reverse'
                                ],
                                formatter: '.formatter.html_for_pollination_status_indicator'
                            }"
                            tooltip="{
                                parts: ['popoverModel>/'],
                                formatter: '.formatter.tooltip_for_pollination_status_indicator'
                            }"
                        /> -->

		<!-- <VBox>
		<Label 
    		text="[{preview_image_model>/plant_id}] {preview_image_model>/plant_name}"
			wrapping="true"
			/>

    	<Image 
			id="idHoverImage"
    		press=".onClickImage"
			src="{preview_image_model>/plant_preview_image_url}"
			/>
		</VBox> -->

    </Popover>
</core:FragmentDefinition>